import { render, screen } from "@testing-library/react";
import { getObjectValue } from "@app/app/utils";

// TODO: Write tests
describe("Utils Tests", () => {
    test("getObjectValue works correctly", () => {});
});
